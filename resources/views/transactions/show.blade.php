<x-app-layout title="Transaction Details">
    <div class="px-4 py-8 sm:px-6 lg:px-8"><div class="mx-auto w-full max-w-5xl space-y-6"><div class="flex items-center justify-between gap-4"><div><h1 class="text-2xl font-bold text-gray-800 dark:text-gray-100 md:text-3xl">{{ ucfirst($transaction->transaction_type) }} Transaction</h1><p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Transaction #{{ $transaction->id }}</p></div><div class="flex gap-3"><a href="{{ route('transactions.index') }}" class="inline-flex items-center rounded-xl border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">Back to Transactions</a>@if ($transaction->service?->isServiceOpen() && $transaction->transaction_type !== 'fill')<a href="{{ route('transactions.edit', $transaction) }}" class="inline-flex items-center rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-medium text-white transition hover:bg-violet-500">Edit Transaction</a>@endif</div></div>@if (session('status'))<div class="rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-700 dark:border-green-900/60 dark:bg-green-500/10 dark:text-green-300">{{ session('status') }}</div>@endif<x-validation-errors /><section class="panel"><div class="panel-body"><dl class="grid gap-4 text-sm md:grid-cols-2 xl:grid-cols-4"><div><dt class="text-gray-500 dark:text-gray-400">Service</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">#{{ $transaction->service_id }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Machine</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ $transaction->machine?->type ?? '—' }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Bin</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ $transaction->bin?->bin_code ?? '—' }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Product</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ $transaction->product?->product_name ?? '—' }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Quantity</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ $transaction->quantity }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Price</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ $transaction->price !== null ? number_format((float) $transaction->price, 2) : '—' }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Unit Cost</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ $transaction->unit_cost !== null ? number_format((float) $transaction->unit_cost, 4) : '—' }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Transaction Date</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ \App\Support\AppDateTime::displayDate($transaction->transaction_at) }}</dd></div><div><dt class="text-gray-500 dark:text-gray-400">Transaction Time</dt><dd class="mt-1 text-gray-800 dark:text-gray-100">{{ \App\Support\AppDateTime::displayTime($transaction->transaction_at) }}</dd></div></dl></div></section></div></div>
</x-app-layout>
