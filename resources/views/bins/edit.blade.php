<x-app-layout title="Edit Bin">
    <div class="px-4 py-8 sm:px-6 lg:px-8"><div class="mx-auto w-full max-w-3xl space-y-6"><div class="flex items-center justify-between gap-4"><div><h1 class="text-2xl font-bold text-gray-800 dark:text-gray-100 md:text-3xl">Edit Bin</h1><p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Update bin details for the selected account.</p></div><a href="{{ route('bins.show', $bin) }}" class="inline-flex items-center rounded-xl border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">Back to Bin</a></div><section class="panel"><div class="panel-body"><form method="POST" action="{{ route('bins.update', $bin) }}" class="space-y-5">@csrf @method('PATCH')<div><x-label for="machine_id" value="Machine" /><select id="machine_id" name="machine_id" class="block w-full rounded-xl border-gray-300 bg-white px-4 py-3 text-sm text-gray-800 shadow-sm focus:border-violet-500 focus:ring-violet-500 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100" required>@foreach ($machines as $machine)<option value="{{ $machine->id }}" @selected(old('machine_id', $bin->machine_id) == $machine->id)>{{ $machine->type }} · {{ $machine->serial_number ?: 'No serial' }} · {{ $machine->location?->location_name ?? 'No location' }}</option>@endforeach</select></div><div><x-label for="product_id" value="Product" /><select id="product_id" name="product_id" class="block w-full rounded-xl border-gray-300 bg-white px-4 py-3 text-sm text-gray-800 shadow-sm focus:border-violet-500 focus:ring-violet-500 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100"><option value="">No product</option>@foreach ($products as $product)<option value="{{ $product->id }}" @selected(old('product_id', $bin->product_id) == $product->id)>{{ $product->product_name }}</option>@endforeach</select></div><div class="grid gap-5 md:grid-cols-3"><div><x-label for="bin_code" value="Bin Code" /><x-input id="bin_code" name="bin_code" type="text" :value="old('bin_code', $bin->bin_code)" required /></div><div><x-label for="capacity" value="Capacity" /><x-input id="capacity" name="capacity" type="number" min="0" :value="old('capacity', $bin->capacity)" required /></div><div><x-label for="price" value="Price" /><x-input id="price" name="price" type="number" min="0" step="0.01" :value="old('price', $bin->price)" /></div></div><div class="flex items-center justify-end gap-3"><a href="{{ route('bins.show', $bin) }}" class="inline-flex items-center rounded-xl border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">Cancel</a><x-button>Save Bin</x-button></div></form><x-validation-errors class="mt-6" /></div></section></div></div>
</x-app-layout>
