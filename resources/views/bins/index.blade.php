<x-app-layout title="Bins">
    <div class="px-4 py-8 sm:px-6 lg:px-8"><div class="mx-auto w-full max-w-7xl space-y-6"><div class="flex items-center justify-between gap-4"><div><h1 class="text-2xl font-bold text-gray-800 dark:text-gray-100 md:text-3xl">Bins</h1><p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Manage machine bins for the selected account.</p></div>@can('create', \App\Models\Bin::class)<a href="{{ route('bins.create') }}" class="inline-flex items-center rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-medium text-white transition hover:bg-violet-500">Add Bin</a>@endcan</div>@if (session('status'))<div class="rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-700 dark:border-green-900/60 dark:bg-green-500/10 dark:text-green-300">{{ session('status') }}</div>@endif<x-validation-errors /><section class="panel"><div class="panel-body border-b border-gray-200 dark:border-gray-700/60"><form method="GET" action="{{ route('bins.index') }}" class="grid gap-4 lg:grid-cols-[1fr_260px_auto]"><x-input name="search" type="text" :value="$search" placeholder="Search bin code" /><select name="machine_id" class="block w-full rounded-xl border-gray-300 bg-white px-4 py-3 text-sm text-gray-800 shadow-sm focus:border-violet-500 focus:ring-violet-500 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100"><option value="">All machines</option>@foreach ($machines as $machine)<option value="{{ $machine->id }}" @selected($selectedMachineId == $machine->id)>{{ $machine->type }} · {{ $machine->serial_number ?: 'No serial' }}</option>@endforeach</select><div class="flex gap-3"><x-button>Filter</x-button><a href="{{ route('bins.index') }}" class="inline-flex items-center rounded-xl border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">Reset</a></div></form></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 text-sm dark:divide-gray-700/60"><thead class="bg-gray-50 dark:bg-gray-800/80"><tr><th class="px-5 py-3 text-left font-medium text-gray-500 dark:text-gray-400">Machine</th><th class="px-5 py-3 text-left font-medium text-gray-500 dark:text-gray-400">Bin Code</th><th class="px-5 py-3 text-left font-medium text-gray-500 dark:text-gray-400">Product</th><th class="px-5 py-3 text-right font-medium text-gray-500 dark:text-gray-400">Capacity</th><th class="px-5 py-3 text-right font-medium text-gray-500 dark:text-gray-400">Price</th><th class="px-5 py-3 text-right font-medium text-gray-500 dark:text-gray-400">Current Inventory</th><th class="px-5 py-3 text-left font-medium text-gray-500 dark:text-gray-400">Actions</th></tr></thead><tbody class="divide-y divide-gray-200 dark:divide-gray-700/60">@forelse ($bins as $bin)<tr class="bg-white dark:bg-gray-800"><td class="px-5 py-4 text-gray-600 dark:text-gray-300">{{ $bin->machine?->type ?? '—' }}</td><td class="px-5 py-4 font-medium text-gray-800 dark:text-gray-100">{{ $bin->bin_code }}</td><td class="px-5 py-4 text-gray-600 dark:text-gray-300">{{ $bin->product?->product_name ?? '—' }}</td><td class="px-5 py-4 text-right tabular-nums text-gray-600 dark:text-gray-300">{{ $bin->capacity }}</td><td class="px-5 py-4 text-right tabular-nums text-gray-600 dark:text-gray-300">{{ $bin->price !== null ? number_format((float) $bin->price, 2) : '—' }}</td><td class="px-5 py-4 text-right tabular-nums text-gray-600 dark:text-gray-300">{{ $inventoryByBin[$bin->id] ?? 0 }}</td><td class="px-5 py-4"><div class="flex flex-wrap gap-2"><a href="{{ route('bins.show', $bin) }}" class="inline-flex items-center rounded-xl border border-gray-300 px-3 py-1.5 text-xs font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">View</a>@can('update', $bin)<a href="{{ route('bins.edit', $bin) }}" class="inline-flex items-center rounded-xl border border-gray-300 px-3 py-1.5 text-xs font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">Edit</a>@endcan @can('delete', $bin)<form method="POST" action="{{ route('bins.destroy', $bin) }}">@csrf @method('DELETE') <button type="submit" class="inline-flex items-center rounded-xl border border-red-300 px-3 py-1.5 text-xs font-medium text-red-700 transition hover:bg-red-50 dark:border-red-500/40 dark:text-red-300 dark:hover:bg-red-500/10">Delete</button></form>@endcan</div></td></tr>@empty<tr class="bg-white dark:bg-gray-800"><td colspan="7" class="px-5 py-8 text-center text-gray-500 dark:text-gray-400">No bins found for this account.</td></tr>@endforelse</tbody></table></div><div class="panel-body">{{ $bins->links() }}</div></section></div></div>
</x-app-layout>
