<x-app-layout title="Add Machine">
    <div class="px-4 py-8 sm:px-6 lg:px-8">
        <div class="mx-auto w-full max-w-3xl space-y-6">
            <div class="flex items-center justify-between gap-4">
                <div><h1 class="text-2xl font-bold text-gray-800 dark:text-gray-100 md:text-3xl">Add Machine</h1><p class="mt-2 text-sm text-gray-500 dark:text-gray-400">Create a machine record for the selected account.</p></div>
                <a href="{{ route('machines.index') }}" class="inline-flex items-center rounded-xl border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">Back to Machines</a>
            </div>
            <section class="panel"><div class="panel-body">@if ($locations->isEmpty())<div class="rounded-xl border border-blue-200 bg-blue-50 px-4 py-3 text-sm text-blue-800 dark:border-blue-900/60 dark:bg-blue-500/10 dark:text-blue-300">No locations exist yet. This machine will be assigned to an auto-created default location when saved.</div>@endif<form method="POST" action="{{ route('machines.store') }}" class="space-y-5">@csrf<div><x-label for="location_id" value="Location" />@if ($locations->isEmpty())<div class="rounded-xl border border-gray-200 bg-gray-50 px-4 py-3 text-sm text-gray-600 dark:border-gray-700/60 dark:bg-gray-800 dark:text-gray-300">Default Location</div>@else<select id="location_id" name="location_id" class="block w-full rounded-xl border-gray-300 bg-white px-4 py-3 text-sm text-gray-800 shadow-sm focus:border-violet-500 focus:ring-violet-500 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100">@foreach ($locations as $location)<option value="{{ $location->id }}" @selected(old('location_id', $defaultLocationId) == $location->id)>{{ $location->location_name }}</option>@endforeach</select>@endif</div><div class="grid gap-5 md:grid-cols-2"><div><x-label for="type" value="Machine Type" /><select id="type" name="type" class="block w-full rounded-xl border-gray-300 bg-white px-4 py-3 text-sm text-gray-800 shadow-sm focus:border-violet-500 focus:ring-violet-500 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100" required><option value="">Select type</option>@foreach ($machineTypes as $machineType)<option value="{{ $machineType }}" @selected(old('type') === $machineType)>{{ ucfirst($machineType) }}</option>@endforeach</select></div><div><x-label for="status" value="Status" /><select id="status" name="status" class="block w-full rounded-xl border-gray-300 bg-white px-4 py-3 text-sm text-gray-800 shadow-sm focus:border-violet-500 focus:ring-violet-500 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100" required>@foreach ($machineStatuses as $machineStatus)<option value="{{ $machineStatus->value }}" @selected(old('status', \App\Models\Machine::STATUS_ACTIVE) === $machineStatus->value)>{{ $machineStatus->displayLabel() }}</option>@endforeach</select></div></div><div class="grid gap-5 md:grid-cols-2"><div><x-label for="serial_number" value="Serial Number" /><x-input id="serial_number" name="serial_number" type="text" :value="old('serial_number')" /></div><div><x-label for="model" value="Model" /><x-input id="model" name="model" type="text" :value="old('model')" /></div></div><div><x-label for="installed_on" value="Installed On" /><x-input id="installed_on" name="installed_on" type="text" placeholder="DD-MM-YYYY" :value="old('installed_on')" /></div><div class="flex items-center justify-end gap-3"><a href="{{ route('machines.index') }}" class="inline-flex items-center rounded-xl border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 transition hover:bg-gray-50 dark:border-gray-600 dark:text-gray-200 dark:hover:bg-gray-700">Cancel</a><x-button>Create Machine</x-button></div></form><x-validation-errors class="mt-6" /></div></section>
        </div>
    </div>
</x-app-layout>
